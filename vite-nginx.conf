server {
    listen 80;

    location = / {
        return 301 /micro_wins/;
    }

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /micro_wins/index.html;
    }

    error_page 404 /micro_wins/index.html;
}
